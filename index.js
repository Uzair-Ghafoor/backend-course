let str = [1, 2, 3, 4, 5, 6, 7];
// adds new string at the start of the array
// str.unshift('uzair');

//removes first index from the array
// str.shift();

//remove the last index of the array
// str.pop();

//adds at the last of the array
// str.push('hello');
